Here we keep templates for scientific papers. Each directory keeps single
template. The name of the directory is the name of template. The invocation::

    bin/newpaper -t foo bar

refers  to a directory ``foo`` here.
